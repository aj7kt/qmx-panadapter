# Keep shell scripts LF even when checked out / zipped on Windows, so the
# macOS/Linux flasher isn't broken by CRLF ("bad interpreter: ...^M").
*.command  text eol=lf
*.sh       text eol=lf

# Windows batch/PowerShell scripts use CRLF.
*.bat      text eol=crlf
*.ps1      text eol=crlf

# Unified diffs must stay byte-exact (their targets are LF; a CRLF checkout
# on Windows would make them fail to apply).
*.patch    -text

# Firmware images and other binaries: never touch them.
*.bin      binary
*.elf      binary
*.png      binary
*.pdf      binary
