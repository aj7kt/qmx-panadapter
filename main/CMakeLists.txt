# Fork policy: main/manual.bin (the embedded user manual) is a build artifact,
# not committed - generate it from docs/mkdocs when missing so a fresh clone
# builds. CI regenerates it unconditionally (.github/workflows/firmware-build.yml);
# pack_manual.py is stdlib-only. Upstream commits the blob instead - if a merge
# ever brings it back, prefer deletion (see docs/fork-additions.md).
if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/manual.bin")
    idf_build_get_property(python PYTHON)
    if(NOT python)
        set(python python3)
    endif()
    message(STATUS "Generating main/manual.bin from docs/mkdocs")
    execute_process(
        COMMAND ${python} "${CMAKE_CURRENT_SOURCE_DIR}/../tools/pack_manual.py"
                --docs "${CMAKE_CURRENT_SOURCE_DIR}/../docs/mkdocs"
                --nav-from-mkdocs "${CMAKE_CURRENT_SOURCE_DIR}/../mkdocs.yml"
                --out "${CMAKE_CURRENT_SOURCE_DIR}/manual.bin"
        RESULT_VARIABLE _pack_manual_rc)
    if(NOT _pack_manual_rc EQUAL 0)
        message(FATAL_ERROR "pack_manual.py failed (rc=${_pack_manual_rc}) - manual.bin could not be generated")
    endif()
endif()

idf_component_register(
    SRCS
        "main.c"
        "display/display.c"
        "ui/ui.c"
        "ui/ui_clock.c"
        "ui/spots_lane.c"
        "ui/help_topics.c"
        "ui/help_triage.c"
        "ui/wifi_config.c"
        "ui/tune_modal.c"
        "ui/activation_modal.c"
        "ui/identity_config.c"
        "ui/onboarding.c"
        "ui/memory_modal.c"
        "ui/ui_mode.c"
        "ui/ft8_screen.c"
        "ui/ft8_screen_view.c"
        "ui/ft8_tx_modal.c"
        "ui/ft8_cq_modal.c"
        "ui/ft8_filter_modal.c"
        "ui/ft8_time_modal.c"
        "ui/ft8_tone_modal.c"
        "ui/adif_view_modal.c"
        "ui/ft8_pileup_modal.c"
        "ui/reader_view.c"
        "ui/qmx_term_view.c"
        "ui/font_qmx_mono_25.c"
        "ui/reader_diagram.c"
        "ui/watermark_img.c"
        "util/status.c"
        "util/maidenhead.c"
    "util/db_gridlines.c"
    "util/ansi_term.c"
        "util/bandplan.c"
        "util/dxcc.c"
        "util/battery.c"
        "util/ina226.c"
        "util/bsp_info.c"
        "util/diag_log.c"
        "util/psram_task.c"
        "util/factory_reset.c"
"util/usb_replug.c"
        "util/cpu_stats.c"
        "cat/cat.c"
        "audio/audio.c"
        "audio/cw_audio.c"
        "dsp/dsp.c"
        "dsp/iq_balance.c"
        "dsp/spur_map.c"
        "render/render.c"
        "render/render_waterfall.c"
        "screenshot/screenshot.c"
        "storage/settings.c"
        "storage/config_io.c"
        "storage/mem_channels.c"
        "storage/sd_archive.c"
        "wifi/wifi.c"
        "util/usb_shutdown.c"
        "net/mdns_svc.c"
        "net/webserver.c"
        "net/webserver_ws.c"
        "net/filebrowser.c"
        "usb_hid_mouse.c"
        "net/rigctld_server.c"
        "net/reader_net.c"
        "net/manual_embed.c"
        "net/update_check.c"
        "qmx_term.c"
        "ft8_test.c"
        "ft8_tx.c"
        "ft8_hash.c"
        "ft8_msg_guard.c"
        "ft8_status.c"
        "ft8_qso.c"
        "ft8_pileup.c"
        "ft8_greylist.c"
        "net/pskreporter.c"
        "net/spots.c"
        "net/spot_sig.c"
        "ft8_hound.c"
        "net/psk_rx.c"
        "hid_cursor.c"
        "bt_hid_mouse.c"
        "hid_report_map.c"
        "net/rbn.c"
        "net/dxcluster.c"
        "ft8_robot.c"
        "ft8_sim.c"
        "adif/adif_log.c"
        "adif/qrz_upload.c"
        "adif/eqsl_upload.c"
        "adif/lotw_tq8.c"
        "adif/lotw_upload.c"
        "rtc/rtc.c"
        "time_sync/time_sync.c"
        "keyboard/tab5_keyboard.c"
    INCLUDE_DIRS
        "."
        "display"
        "ui"
        "util"
        "cat"
        "audio"
        "dsp"
        "render"
        "screenshot"
        "storage"
        "wifi"
        "net"
        "rtc"
        "time_sync"
        "adif"
        "keyboard"
    REQUIRES
        esp_lcd
        esp_timer
        driver
        usb
        lvgl__lvgl
        m5stack_tab5
        espressif__esp_lcd_touch
        espressif__usb_host_cdc_acm
        espressif__usb_host_uac
        espressif__esp-dsp
        nvs_flash
        esp_wifi
        esp_netif
        esp_event
        lwip
        esp_http_server
        esp_http_client
        espressif__mdns
        mbedtls
        json
        ft8_lib
        spiffs
        bt            # NimBLE host for the BLE mouse (controller is on the C6)
        esp_hid
    PRIV_REQUIRES
        esp_app_format
        fatfs
    EMBED_TXTFILES
        "net/www/index.html"
    EMBED_FILES
        "net/www/forge.min.js.gz"
        # User manual, built into the binary so it is available with no WiFi, no
        # SD card and no download - and can never document a different firmware
        # version than the one running. Regenerated by tools/pack_manual.py, which
        # mkdocs_reader_export.py runs during `mkdocs build`; committed so the
        # firmware build needs no Python/mkdocs of its own. ~136 KB of an 8 MB app
        # partition, and zero SPIFFS. See net/manual_embed.h.
        "manual.bin"
)

# Route LVGL's 256 KB builtin tlsf pool from internal SRAM BSS to PSRAM.
# LV_MEM_POOL_ALLOC is defined in lv_pool_shim.h and force-included into
# every LVGL translation unit so lv_mem_core_builtin.c uses it instead of
# the static work_mem_int[] BSS array.  This returns ~256 KB to IDF's
# internal heap without changing the tlsf allocator or touching Kconfig.
idf_component_get_property(lvgl_lib lvgl__lvgl COMPONENT_LIB)
target_compile_options(${lvgl_lib} PRIVATE
    "-include${CMAKE_CURRENT_SOURCE_DIR}/util/lv_pool_shim.h"
)
# ---------------------------------------------------------------------------
# Refuse to build with a standing patch missing (TODO #181).
#
# Six patches live OUTSIDE version control - three in git-ignored
# managed_components/, three in the pinned ESP-IDF install tree - so a clean
# fetch, an `idf.py fullclean`, a dependency refresh or an IDF reinstall removes
# them silently. The binary then builds perfectly and fails on hardware in a way
# that reads as a hardware fault: on 2026-08-17 three of them were missing and
# WiFi died within ~4 minutes of every boot, which cost an evening and two wrong
# hypotheses before anyone checked. See TODO #180.
#
# PRE_BUILD on the component lib, not configure-time: a patch can vanish after
# CMake has run, and this must catch that too. The check is a few file reads.
add_custom_command(
    TARGET ${COMPONENT_LIB} PRE_BUILD
    COMMAND ${CMAKE_COMMAND} -E env "IDF_PATH=$ENV{IDF_PATH}"
            ${PYTHON} "${CMAKE_CURRENT_SOURCE_DIR}/../tools/check_patches.py"
    COMMENT "Checking standing patches (managed_components + IDF tree)"
    VERBATIM
)
