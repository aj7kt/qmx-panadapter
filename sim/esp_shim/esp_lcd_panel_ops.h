#pragma once
// Tier-1 shim: main/display/display.h includes this but (in the subset of
// the app built here) never names a type or function from it directly -
// the include only needs to succeed, not provide real declarations.
